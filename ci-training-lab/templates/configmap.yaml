apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ci-training-lab.fullname" . }}
  labels:
    {{- include "ci-training-lab.labels" . | nindent 4 }}
data:
  NODE_ENV: {{ .Values.env.NODE_ENV | quote }}
  PORT: {{ .Values.env.PORT | quote }}
